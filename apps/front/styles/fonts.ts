// eslint-disable-next-line camelcase
import { Bungee_Hairline, Raleway } from 'next/font/google'

export const bungee = Bungee_Hairline({ weight: '400', subsets: ['latin'] })

export const raleway = Raleway({ subsets: ['latin'] })
